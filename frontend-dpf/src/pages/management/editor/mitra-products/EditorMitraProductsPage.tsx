import { useEffect, useState } from "react";
import { useNavigate } from "react-router-dom";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faPenToSquare, faPlus, faTrash } from "@fortawesome/free-solid-svg-icons";
import http from "@/lib/http";
import { useToast } from "@/components/ui/ToastProvider";
import { resolveMitraProductImage, statusLabel, type MitraProduct } from "@/components/management/editor/mitra-products/MitraProductTypes";
type Response = { data: MitraProduct[]; current_page: number; last_page: number; total: number };
export default function EditorMitraProductsPage() { const navigate = useNavigate(); const toast = useToast(); const [items, setItems] = useState<MitraProduct[]>([]); const [loading, setLoading] = useState(true); const [page, setPage] = useState(1); const [pagination, setPagination] = useState({ current_page: 1, last_page: 1, total: 0 }); const [status, setStatus] = useState(''); const [search, setSearch] = useState(''); const [error, setError] = useState<string | null>(null); const [deleting, setDeleting] = useState<number | null>(null);
  const load = async () => { setLoading(true); try { const response = await http.get<Response>('/editor/mitra-products', { params: { page, per_page: 15, status: status || undefined, q: search || undefined } }); setItems(response.data.data ?? []); setPagination({ current_page: response.data.current_page, last_page: response.data.last_page, total: response.data.total }); setError(null); } catch { setError('Gagal memuat produk mitra.'); } finally { setLoading(false); } };
  // eslint-disable-next-line react-hooks/exhaustive-deps
  useEffect(() => { load(); }, [page, status]);
  const remove = async (id: number) => { if (!window.confirm('Hapus produk dan semua gambarnya secara permanen?')) return; setDeleting(id); try { await http.delete(`/editor/mitra-products/${id}`); toast.success('Produk mitra berhasil dihapus.', { title: 'Berhasil' }); load(); } catch { toast.error('Gagal menghapus produk mitra.', { title: 'Gagal' }); } finally { setDeleting(null); } };
  return <div className="mx-auto w-full max-w-7xl space-y-6 pb-12"><div className="flex flex-col gap-4 rounded-[28px] border border-slate-200 border-l-4 border-brandGreen-400 bg-white p-6 shadow-sm sm:flex-row sm:items-center sm:justify-between"><div><p className="text-[11px] font-bold uppercase tracking-[0.2em] text-brandGreen-600">Konten</p><h1 className="mt-2 font-heading text-2xl font-semibold text-slate-900 sm:text-3xl">Produk Mitra</h1><p className="mt-2 text-sm text-slate-600">Kelola produk publik dan kontak WhatsApp mitra.</p>{!loading && <p className="mt-3 text-xs font-bold text-slate-400">{pagination.total} item</p>}</div><button type="button" onClick={() => navigate('/editor/mitra-products/create')} className="inline-flex items-center gap-2 rounded-2xl bg-brandGreen-600 px-5 py-3 text-sm font-bold text-white"><FontAwesomeIcon icon={faPlus} />Tambah Produk</button></div><form onSubmit={(event) => { event.preventDefault(); setPage(1); load(); }} className="flex flex-col gap-3 rounded-[28px] border border-slate-200 bg-white p-4 shadow-sm sm:flex-row"><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Cari produk atau mitra..." className="min-w-0 flex-1 rounded-2xl border border-slate-200 px-4 py-3 text-sm font-semibold" /><select value={status} onChange={(event) => { setStatus(event.target.value); setPage(1); }} className="rounded-2xl border border-slate-200 px-4 py-3 text-sm font-semibold"><option value="">Semua status</option><option value="draft">Draf</option><option value="published">Terbit</option><option value="archived">Arsip</option></select><button className="rounded-2xl bg-slate-900 px-5 py-3 text-sm font-bold text-white">Cari</button></form>{error && <p className="rounded-2xl bg-rose-50 p-4 text-sm font-semibold text-rose-700">{error}</p>}<div className="overflow-x-auto rounded-[28px] border border-slate-200 bg-white shadow-sm"><table className="min-w-full text-left"><thead className="bg-slate-50 text-[11px] uppercase tracking-[0.18em] text-slate-500"><tr><th className="px-5 py-4">Gambar</th><th className="px-5 py-4">Produk</th><th className="px-5 py-4">Mitra</th><th className="px-5 py-4">Status</th><th className="px-5 py-4">Aksi</th></tr></thead><tbody className="divide-y divide-slate-100">{loading ? <tr><td colSpan={5} className="p-10 text-center text-sm text-slate-400">Memuat...</td></tr> : items.length === 0 ? <tr><td colSpan={5} className="p-10 text-center text-sm text-slate-500">Belum ada produk mitra.</td></tr> : items.map((item) => <tr key={item.id} className="hover:bg-slate-50"><td className="px-5 py-4"><img src={resolveMitraProductImage(item.images?.[0]?.image)} alt="" className="h-14 w-20 rounded-xl object-cover ring-1 ring-slate-200" /></td><td className="px-5 py-4"><p className="font-bold text-slate-900">{item.title_id}</p><p className="text-xs text-slate-500">/{item.slug}</p></td><td className="px-5 py-4 text-sm font-semibold text-slate-600">{item.nama_mitra || '-'}</td><td className="px-5 py-4 text-sm font-bold text-slate-600">{statusLabel(item.status)}</td><td className="px-5 py-4"><div className="flex gap-2"><button type="button" onClick={() => navigate(`/editor/mitra-products/${item.id}/edit`)} className="rounded-xl border border-slate-200 p-2 text-slate-600"><FontAwesomeIcon icon={faPenToSquare} /></button><button type="button" disabled={deleting === item.id} onClick={() => remove(item.id)} className="rounded-xl bg-rose-50 p-2 text-rose-600 disabled:opacity-50"><FontAwesomeIcon icon={faTrash} /></button></div></td></tr>)}</tbody></table></div>{pagination.last_page > 1 && <div className="flex items-center justify-between text-sm font-bold text-slate-600"><button type="button" disabled={page <= 1} onClick={() => setPage((value) => value - 1)} className="rounded-xl px-4 py-2 disabled:opacity-40">Sebelumnya</button><span>{pagination.current_page} / {pagination.last_page}</span><button type="button" disabled={page >= pagination.last_page} onClick={() => setPage((value) => value + 1)} className="rounded-xl px-4 py-2 disabled:opacity-40">Berikutnya</button></div>}</div>; }
