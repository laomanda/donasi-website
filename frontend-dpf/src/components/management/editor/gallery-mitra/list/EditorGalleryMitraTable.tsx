import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faPenToSquare, faTrash } from "@fortawesome/free-solid-svg-icons";
import { imagePlaceholder } from "@/lib/placeholder";
import { formatGalleryMitraDate, getGalleryMitraStatusLabel, getGalleryMitraStatusTone, resolveGalleryMitraUrl, type GalleryMitra } from "../GalleryMitraTypes";
type Props = { items: GalleryMitra[]; loading: boolean; onEdit: (id: number) => void; onDelete: (item: GalleryMitra) => void; confirmDeleteId: number | null; setConfirmDeleteId: (id: number | null) => void; deletingId: number | null };
export default function EditorGalleryMitraTable({ items, loading, onEdit, onDelete, confirmDeleteId, setConfirmDeleteId, deletingId }: Props) { return <div className="overflow-hidden rounded-[28px] border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="min-w-full text-left"><thead className="border-b border-slate-200 bg-slate-50"><tr>{["Gambar", "Caption", "Status", "Diperbarui", "Aksi"].map((label) => <th key={label} className="px-5 py-4 text-[11px] font-bold uppercase tracking-[0.18em] text-slate-500">{label}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{loading ? Array.from({ length: 4 }).map((_, index) => <tr key={index} className="animate-pulse"><td className="px-5 py-5"><div className="h-14 w-20 rounded-xl bg-slate-100" /></td><td className="px-5 py-5"><div className="h-4 w-48 rounded bg-slate-100" /></td><td className="px-5 py-5"><div className="h-6 w-20 rounded-full bg-slate-100" /></td><td className="px-5 py-5"><div className="h-4 w-24 rounded bg-slate-100" /></td><td /></tr>) : items.length === 0 ? <tr><td colSpan={5} className="px-6 py-12 text-center text-sm font-semibold text-slate-500">Belum ada aktivitas mitra.</td></tr> : items.map((item) => <tr key={item.id} className="hover:bg-slate-50"><td className="px-5 py-4"><div className="h-14 w-20 overflow-hidden rounded-xl bg-slate-50 ring-1 ring-slate-200"><img src={resolveGalleryMitraUrl(item.image)} alt="Aktivitas Mitra" className="h-full w-full object-cover" onError={(event) => { event.currentTarget.src = imagePlaceholder; }} /></div></td><td className="px-5 py-4"><p className="text-sm font-bold text-slate-900">{item.caption_id}</p><p className="mt-1 text-xs text-slate-500">{item.caption_en}</p></td><td className="px-5 py-4"><span className={`inline-flex rounded-full px-3 py-1 text-[10px] font-bold uppercase ring-1 ${getGalleryMitraStatusTone(item.status)}`}>{getGalleryMitraStatusLabel(item.status)}</span></td><td className="px-5 py-4 text-sm font-semibold text-slate-600">{formatGalleryMitraDate(item.updated_at ?? item.created_at)}</td><td className="px-5 py-4"><div className="flex gap-2"><button type="button" onClick={() => onEdit(item.id)} aria-label="Ubah aktivitas mitra" className="inline-flex h-9 w-9 items-center justify-center rounded-xl border border-slate-200 text-slate-600 hover:text-brandGreen-600"><FontAwesomeIcon icon={faPenToSquare} /></button><button type="button" onClick={() => setConfirmDeleteId(confirmDeleteId === item.id ? null : item.id)} aria-label="Hapus aktivitas mitra" className="inline-flex h-9 w-9 items-center justify-center rounded-xl border border-rose-100 text-rose-500 hover:bg-rose-50"><FontAwesomeIcon icon={faTrash} /></button></div>{confirmDeleteId === item.id && <div className="mt-3 flex min-w-64 items-center gap-2 rounded-xl border border-rose-200 bg-rose-50 p-3"><button type="button" onClick={() => onDelete(item)} disabled={deletingId === item.id} className="rounded-lg bg-rose-600 px-3 py-2 text-xs font-bold text-white disabled:opacity-50">{deletingId === item.id ? "Menghapus..." : "Hapus permanen"}</button><button type="button" onClick={() => setConfirmDeleteId(null)} className="rounded-lg px-3 py-2 text-xs font-bold text-slate-600">Batal</button></div>}</td></tr>)}</tbody></table></div></div>; }
