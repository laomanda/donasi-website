import { useState } from "react";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faArrowLeft, faArrowRight } from "@fortawesome/free-solid-svg-icons";
import { imagePlaceholder } from "@/lib/placeholder";
import { resolveStorageUrl } from "@/lib/urls";
type Props = { images: { image: string }[]; title: string };
export default function MitraProductGallery({ images, title }: Props) { const [active, setActive] = useState(0); const urls = images.map((item) => resolveStorageUrl(item.image, imagePlaceholder) ?? imagePlaceholder); const image = urls[active] ?? imagePlaceholder; return <div className="overflow-hidden rounded-[28px] border border-slate-100 bg-slate-100 shadow-soft"><div className="relative aspect-[4/3]"><img src={image} alt={title} className="h-full w-full object-contain" onError={(event) => { event.currentTarget.src = imagePlaceholder; }} />{urls.length > 1 && <><button type="button" onClick={() => setActive((index) => (index - 1 + urls.length) % urls.length)} className="absolute left-4 top-1/2 -translate-y-1/2 rounded-full bg-white/90 px-3 py-2 text-slate-700"><FontAwesomeIcon icon={faArrowLeft} /></button><button type="button" onClick={() => setActive((index) => (index + 1) % urls.length)} className="absolute right-4 top-1/2 -translate-y-1/2 rounded-full bg-white/90 px-3 py-2 text-slate-700"><FontAwesomeIcon icon={faArrowRight} /></button></>}</div>{urls.length > 1 && <div className="flex justify-center gap-2 border-t bg-white p-3">{urls.map((_, index) => <button key={index} type="button" aria-label={`Gambar ${index + 1}`} onClick={() => setActive(index)} className={`h-2.5 w-2.5 rounded-full ${index === active ? 'bg-brandGreen-600' : 'bg-slate-300'}`} />)}</div>}</div>; }
