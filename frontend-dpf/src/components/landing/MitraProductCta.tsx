import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faArrowRight, faStore } from "@fortawesome/free-solid-svg-icons";
import { Link } from "react-router-dom";
import type { Locale } from "@/lib/i18n";
export function MitraProductCta({ locale }: { locale: Locale }) { const en = locale === 'en'; return <section className="bg-white py-16 sm:py-20"><div className="mx-auto max-w-5xl px-4 sm:px-6 lg:px-8"><div className="flex flex-col items-start justify-between gap-6 rounded-[32px] bg-gradient-to-br from-amber-600 via-orange-600 to-primary-600 px-6 py-8 text-white shadow-2xl sm:flex-row sm:items-center sm:px-10"><div className="flex gap-4"><span className="flex h-12 w-12 items-center justify-center rounded-2xl bg-white/15"><FontAwesomeIcon icon={faStore} /></span><div><p className="text-xs font-bold uppercase tracking-[0.2em] text-white/70">{en ? 'Partner products' : 'Produk mitra'}</p><h2 className="mt-2 font-heading text-2xl font-bold">{en ? 'Discover partner products' : 'Jelajahi produk mitra'}</h2></div></div><Link to="/produk-mitra" className="inline-flex items-center gap-2 rounded-full bg-white px-6 py-3 text-sm font-bold text-primary-700">{en ? 'View products' : 'Lihat produk'}<FontAwesomeIcon icon={faArrowRight} /></Link></div></div></section>; }
